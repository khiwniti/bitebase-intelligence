{
  "name": "beta-bitebase-intelligence",
  "private": true,
  "description": "BiteBase Intelligence - Unified monorepo for frontend and backend applications",
  "scripts": {
    "build": "yarn turbo run build",
    "dev": "yarn turbo run dev",
    "dev:frontend": "yarn turbo run dev --filter=bitebase-frontend",
    "dev:backend": "yarn turbo run dev --filter=bitebase-backend",
    "dev:all": "yarn turbo run dev --parallel",
    "start": "yarn turbo run start",
    "start:frontend": "yarn turbo run start --filter=bitebase-frontend",
    "start:backend": "yarn turbo run start --filter=bitebase-backend",
    "start:all": "yarn turbo run start --parallel",
    "staging": "yarn turbo run staging",
    "staging:frontend": "yarn turbo run staging --filter=bitebase-frontend",
    "staging:backend": "yarn turbo run staging --filter=bitebase-backend",
    "staging:all": "yarn turbo run staging --parallel",
    "lint": "yarn turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,md,json}\"",
    "check-types": "yarn turbo run check-types",
    "test": "yarn turbo run test",
    "clean": "yarn turbo run clean",
    "setup": "yarn install && yarn build",
    "install:all": "yarn install",
    "clean:all": "yarn clean && rm -rf node_modules yarn.lock"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.5.1"
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/khiwniti/bitebase-intelligence.git"
  },
  "keywords": [
    "monorepo",
    "turborepo",
    "bitebase",
    "restaurant",
    "geospatial",
    "saas"
  ],
  "packageManager": "yarn@1.22.22",
  "author": "BiteBase Team",
  "license": "MIT",
  "dependencies": {
    "mapbox-gl": "^3.12.0"
  }
}