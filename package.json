{
  "name": "beta-bitebase-intelligence",
  "private": true,
  "description": "BiteBase Intelligence - Unified monorepo for frontend and backend applications",
  "scripts": {
    "build": "npx turbo run build",
    "dev": "npx turbo run dev",
    "dev:frontend": "npx turbo run dev --filter=bitebase-frontend",
    "dev:backend": "npx turbo run dev --filter=bitebase-backend",
    "dev:all": "npx turbo run dev --parallel",
    "start": "npx turbo run start",
    "start:frontend": "npx turbo run start --filter=bitebase-frontend",
    "start:backend": "npx turbo run start --filter=bitebase-backend",
    "start:all": "npx turbo run start --parallel",
    "staging": "npx turbo run staging",
    "staging:frontend": "npx turbo run staging --filter=bitebase-frontend",
    "staging:backend": "npx turbo run staging --filter=bitebase-backend",
    "staging:all": "npx turbo run staging --parallel",
    "lint": "npx turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,md,json}\"",
    "check-types": "npx turbo run check-types",
    "test": "npx turbo run test",
    "clean": "npx turbo run clean",
    "setup": "yarn install && yarn build",
    "install:all": "yarn install",
    "clean:all": "yarn clean && rm -rf node_modules yarn.lock"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.5.1"
  },
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/khiwniti/bitebase-intelligence.git"
  },
  "keywords": [
    "monorepo",
    "turborepo",
    "bitebase",
    "restaurant",
    "geospatial",
    "saas"
  ],
  "packageManager": "yarn@1.22.22",
  "author": "BiteBase Team",
  "license": "MIT",
  "dependencies": {
    "mapbox-gl": "^3.12.0"
  }
}